-- Creates a table first_table with the following columns.
--	id INT
-- 	name VARCHAR(256)
-- The database name will be passed as an argument of the mysql command.
CREATE TABLE IF NOT EXISTS first_table (
	id INT,
	name VARCHAR(256)
);
